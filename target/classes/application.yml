server:
  error:
    include-message: always
    include-binding-errors: always

spring:
  datasource:
    password: 2b7a555f9
    url: jdbc:postgresql://localhost:5432/runningevents
    username: citizen
    hikari:
      auto-commit: false
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

  mail:
    host: smtp.gmail.com
    port: 25
    username: runningeventsmail@gmail.com
    password: nbqgybgrpfqhofyv

    properties:
      mail:
        smtp:
          ssl:
            trust: "*"
          auth: true
          starttls:
            enable: true
